# The all-or-nothing assignment step, where all vehicles traveling from A to B are assigned to the same route. This is done repeatedly with new weights and averaged to distribute vehicles across the network.

The all-or-nothing assignment step, where all vehicles traveling from A
to B are assigned to the same route. This is done repeatedly with new
weights and averaged to distribute vehicles across the network.

## Usage

``` r
all_or_nothing(nodeflow_hourly, marginals, network, weights)
```
