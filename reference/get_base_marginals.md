# This gets baseline marginals for a region, from the 5-year ACS

This gets baseline marginals for a region, from the 5-year ACS

## Usage

``` r
get_base_marginals(state, county = NULL, year = NULL)
```
