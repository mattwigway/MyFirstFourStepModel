# This adjusts the total attraction numbers to match total production. It returns a list with members attractions containing the balanced attractions, production containing the original productions, and factors containing the adjustment factors

This adjusts the total attraction numbers to match total production. It
returns a list with members attractions containing the balanced
attractions, production containing the original productions, and factors
containing the adjustment factors

## Usage

``` r
balance_production_attraction(production, attraction)
```
