# We use a singly-constrained (at origin) gravity model. For simplicity, we use straight-line (great circle) distance between points. This function estimates that distance matrix.

We use a singly-constrained (at origin) gravity model. For simplicity,
we use straight-line (great circle) distance between points. This
function estimates that distance matrix.

## Usage

``` r
get_distance_matrix(marginals)
```
