# This generates attraction estimates from LODES data Marginals are only used to get the list of tracts, since LODES does not contain all tracts

This generates attraction estimates from LODES data Marginals are only
used to get the list of tracts, since LODES does not contain all tracts

## Usage

``` r
get_attraction_counts(marginals, attraction_functions)
```
