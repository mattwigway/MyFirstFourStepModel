# Perform the singly-constrained gravity model to get zone-to-zone flows based on balanced trip tables and estimated betas from above.

Perform the singly-constrained gravity model to get zone-to-zone flows
based on balanced trip tables and estimated betas from above.

## Usage

``` r
get_flows(balanced, marginals, betas)
```
