# This estimates the distribution models on the Seattle data It expects LODES data already aggregated to tracts

This estimates the distribution models on the Seattle data It expects
LODES data already aggregated to tracts

## Usage

``` r
estimate_attraction_functions(trips, lodes)
```
