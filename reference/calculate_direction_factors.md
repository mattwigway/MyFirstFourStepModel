# Up through the mode choice step, everything is done with productions at home and attractions at work. For assignment, we need origins and destinations. This calculates factors to figure out how many trips of each home-based type travel in each direction during a particular time period

Up through the mode choice step, everything is done with productions at
home and attractions at work. For assignment, we need origins and
destinations. This calculates factors to figure out how many trips of
each home-based type travel in each direction during a particular time
period

## Usage

``` r
calculate_direction_factors(nhts)
```
