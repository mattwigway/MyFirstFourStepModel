# This function uses the output of [`mode_choice()`](https://projects.indicatrix.org/MyFirstFourStepModel/reference/mode_choice.md) to calculate mode shares, returning a data frame with rows for car, bike, walk, and transit.

This function uses the output of
[`mode_choice()`](https://projects.indicatrix.org/MyFirstFourStepModel/reference/mode_choice.md)
to calculate mode shares, returning a data frame with rows for car,
bike, walk, and transit.

## Usage

``` r
get_mode_shares(flows_by_mode)
```
