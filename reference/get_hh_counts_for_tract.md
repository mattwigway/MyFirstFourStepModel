# This generates the number of households in each category for a specific geographic area (e.g. tract).

This generates the number of households in each category for a specific
geographic area (e.g. tract).

## Usage

``` r
get_hh_counts_for_tract(grp, id, seed)
```
